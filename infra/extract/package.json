{
  "name": "reel-lens-extract",
  "version": "0.1.0",
  "private": true,
  "description": "Keyframe extraction Lambda (container image with ffmpeg)",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.669.0",
    "@aws-sdk/client-s3": "^3.669.0",
    "@aws-sdk/lib-dynamodb": "^3.669.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
